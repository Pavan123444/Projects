str = [5, 3, 8, 4, 2]
for i in range(1, len(arr)):
    key = arr[i]
    j = i - 1
  while j >= 0 and key < str[j]:
      str[j + 1] = str[j]
        j = j - 1
       str[j + 1] = key
print("Sorted array:", str)
